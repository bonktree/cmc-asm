[sdl]
fullresolution=desktop
output=overlay
autolock=false

[autoexec]
@echo off
keyb ru 866
# user source directory, rw
mount u .
# the directory where cmc-masm package is located
# possibly read-only (NFS /usr, for instance)
# to be set at runtime
mount c -package-masm-dir-
u:
rkm -x
cls

